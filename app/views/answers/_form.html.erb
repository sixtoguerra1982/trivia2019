<%= form_with(model: @answer, local: true) do |form| %>

  <div class="field">
    <%= form.label :answer, "Respuesta" %>
    <%= form.text_field :answer %>
  </div>
  <%= form.select :question_id, @questions.pluck(:description, :id) %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
